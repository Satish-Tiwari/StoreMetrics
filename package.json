{
  "name": "store-metrics-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm run build --prefix src/main/frontend",
    "build:backend": "mvn clean package",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "mvn spring-boot:run",
    "dev:frontend": "npm run dev --prefix src/main/frontend"
  }
}
